{{>layouts/header}}

<div class="container mt-5">
    <div class="text-center my-4">
        <h3 class="fw-bold" style="font-size: 2rem; color: #4A90E2;">
        <span style="border-bottom: 3px solid #4A90E2; padding-bottom: 5px;">
            <i class="bi bi-search"></i> 검색어
        </span>
        </h3>
        <br>
        <p class="mt-2 fs-4 text-muted" style="font-style: italic;">
            "{{keyword}}"에 대한 결과를 보여드립니다.
        </p>
        <br>
    </div>

    <div class="row g-4">
        {{#products}}
            <div class="col-md-6 col-lg-4">
                <!-- Product Card -->
                <div class="card shadow-sm border-0 h-100">
                    <img src="../images/{{pimg}}" class="card-img-top" alt="{{pname}}" style="height: 200px; object-fit: cover;">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title text-truncate">{{pname}}</h5>
                        <p class="card-text text-muted">{{pprice}} 원</p>
                        <a href="/products/{{id}}" class="btn btn-primary mt-auto">자세히 보기</a>
                    </div>
                </div>
            </div>
        {{/products}}

        {{^products}}
            <div class="col-12">
                <div class="alert alert-warning text-center" role="alert">
                    <p class="mb-0">검색된 상품이 없습니다.</p>
                </div>
            </div>
        {{/products}}
    </div>
</div>

{{>layouts/footer}}
